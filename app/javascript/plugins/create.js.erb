<% if @message.errors.any? %>

<% else %>
  const messageContainer = document.querySelector('.messages-all');
  messageContainer.insertAdjacentHTML('beforeend', '<%= j render "message", message: @message %>')
  const input = document.querySelector('.message-content');
  input.value = '';
  input.blur();
<% end %>
