
function upvote() {

  const voteId = <%= @report.id %>;

  const counter = document.querySelector(`.product-upvote[data-report-id="${voteId}"]`);

  // take how many likes

  //parse to int

  // + 1

  // update the div innertEXT

  const totalVotes = <%= @report.votes.count %>;
  const countSpan = document.querySelector(`span[data-report-id="${voteId}"]`)
  countSpan.innerText = totalVotes;

  // remove 'can-vote' class from thumb element
  // const voteThumb = document.querySelector(`.product-details[data-report-id="${voteId}"] .can-vote`);
  // voteThumb.classList.remove('can-vote');

  // counter.innerText = "voted"
}

upvote()
